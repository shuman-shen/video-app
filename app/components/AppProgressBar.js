import ProgressBar from "react-native-progress/Bar";
import React from "react";

const AppProgressBar = () => {
  return <ProgressBar indeterminate width={350} borderWidth={0} />;
};

export default AppProgressBar;
